import 'package:flutter/material.dart';

class HumanBodyScreen extends StatelessWidget {
  const HumanBodyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
