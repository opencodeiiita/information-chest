import 'package:flutter/material.dart';

class WorldHistoryScreen extends StatelessWidget {
  const WorldHistoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
